# CAPACITY_GUIDE

当前美股多标的 `IC` 实盘系统的资金规模、组数和预期成交难度对照如下。

前提：

- 实际本金按 `IC_MANUAL_CAPITAL`
- 杠杆固定 `2x`
- 动态配置基准：`P3.0% / C6.0% / Wing9% / DTE45`
- 组数公式：`base_groups × (actual_capital × 2 / 30000)`
- 基线组数：`QQQ=4 / IWM=2 / GLD=2`
- 当前执行方式：**四条腿串行单独下单**

## 资金规模对照

| 实际资金 | 名义资金 | QQQ组数 | IWM组数 | GLD组数 | 总组数 | 总腿数 | 预期成交难度 | 建议 |
|---:|---:|---:|---:|---:|---:|---:|---|---|
| `$15k` | `$30k` | `4` | `2` | `2` | `8` | `32` | 很低 | 当前基准 |
| `$25k` | `$50k` | `6` | `3` | `3` | `12` | `48` | 很低 | 可直接沿用 `F=20x` |
| `$50k` | `$100k` | `13` | `6` | `6` | `25` | `100` | 低 | 仍可用 `F=20x` |
| `$75k` | `$150k` | `20` | `10` | `10` | `40` | `160` | 中低 | 建议分批 |
| `$100k` | `$200k` | `26` | `13` | `13` | `52` | `208` | 中等 | 一般仍可做 |
| `$150k` | `$300k` | `40` | `20` | `20` | `80` | `320` | 中高 | 开始关注 IWM/GLD 腿流动性 |
| `$200k` | `$400k` | `53` | `26` | `26` | `105` | `420` | 高 | 建议自动降到 `E=15x` |
| `$250k` | `$500k` | `66` | `33` | `33` | `132` | `528` | 很高 | `E=15x` + 分批 + 流动性闸门 |
| `$300k` | `$600k` | `80` | `40` | `40` | `160` | `640` | 极高 | 已到当前 `F=20x` 代码硬上限 |

## 当前判断

- **`$100k` 以下**：通常没有明显流动性问题，更多是个别腿点差波动问题
- **`$100k ~ $200k`**：可继续做大，但建议引入分批开仓
- **`$200k` 以上**：建议把 `F=20x` 自动降成 `E=15x`
- **`$300k` 以上**：不建议在现有“四腿串行单独下单”架构下继续放大

## 当前系统升级方案

- 流动性闸门：卖腿无有效 `bid` / 盘口无深度时跳过
- 分批开仓：按标的拆批执行，避免一次性把盘口打穿
- 规模降档：`IC_MANUAL_CAPITAL >= $200k` 时，自动从 `F=20x` 降到 `E=15x`
- 容量预警：`IC_MANUAL_CAPITAL >= $300k` 时，明确提示接近当前系统舒适上限

## 关键结论

- **`$200k ~ $300k`**：本质上还是“组数 + 执行”问题，核心结构 `P3/C6/W9/D45` 不必改
- **`$500k` 以上**：已经不再只是组数问题，而是**执行架构 + 组合架构**问题
- **`$1M ~ $3M`**：不建议继续把同一批次、同一到期、同一四腿串行执行线性放大；应改为多梯队、多 sleeve、多执行通道

## 扩容表（当前代码有效口径）

说明：

- 下表以**当前实盘代码**为准
- `IC_MANUAL_CAPITAL >= $200k` 后，系统会自动从 `F=20x` 降到 `E=15x`
- 所以 `500k / 1M / 3M` 在**当前代码下**，单周期预计组数不会再线性增加
- 真正的进一步扩容，必须靠**策略架构升级**，而不是继续堆单期组数

| 实际资金 | 名义资金 | 当前有效 cap | QQQ组数 | IWM组数 | GLD组数 | 总组数 | 总腿数 | 执行方案 |
|---:|---:|---|---:|---:|---:|---:|---:|---|
| `$200k` | `$400k` | `E=15x` | `53` | `26` | `26` | `105` | `420` | 保持单一策略，严格分批；QQQ 可相对主承载，IWM/GLD 强化腿级流动性闸门 |
| `$300k` | `$600k` | `E=15x` | `60` | `30` | `30` | `120` | `480` | 视为当前单周期舒适上限；同到期集中度要受限，建议拆成多批次/多时点执行 |
| `$500k` | `$1.0M` | `E=15x` | `60` | `30` | `30` | `120` | `480` | 不再加单期组数；改为“到期梯队 + 周内错峰 + 标的 sleeve”扩容 |
| `$1M` | `$2.0M` | `E=15x` | `60` | `30` | `30` | `120` | `480` | 核心 IC 只做一个 sleeve；其余资金用多到期分层、分账户/分执行通道承接 |
| `$3M` | `$6.0M` | `E=15x` | `60` | `30` | `30` | `120` | `480` | 当前架构不适合直接承载；必须升级为组合化执行系统，再决定每个 sleeve 的容量预算 |

## `$500k` 以上：不是组数问题，而是策略升级问题

我同意这个判断。到 `500k` 以上，真正限制你的，不再是回测里的 `F/E` 参数，而是以下三个层面：

### 1) 执行架构升级

- 从“四腿串行单独下单”升级到**价差单 / 组合单优先**
- 同一标的改成**分批、分时、分到期日**开仓，避免把成交冲击集中在一刻
- 增加**单腿限额 / 单次新增腿数 / 单日新增名义资金**上限
- 对 `IWM / GLD` 维持比 `QQQ` 更严格的盘口深度要求

### 2) 组合架构升级

- 不再把所有容量压在**同一 DTE、同一到期批次**
- 改为 **多梯队 IC sleeve**：
  - 主 sleeve：`DTE45`，维持当前 `P3/C6/W9`
  - 执行 sleeve：错峰分布到不同开仓日/不同到期周
  - 风控 sleeve：保留更高现金缓冲，不追求满载
- 核心目标是降低**同日建仓、同日止损、同日到期**的同步性

### 3) 资金治理升级

- `500k ~ 1M`：重点是**提高资金周转的平滑度**
- `1M ~ 3M`：重点是**容量预算管理**，不是参数微调
- 需要给每个标的、每个到期梯队、每个执行时段设置独立容量上限
- 到这个规模后，继续追求回测上的最高年化，通常会明显伤害实盘 `Sharpe` 和回撤控制

## 我建议的实盘扩容路线

### 阶段 A：`$200k ~ $300k`

- 保持当前结构参数不动：`P3/C6/W9/D45`
- 执行上自动降到 `E=15x`
- 强制分批执行
- 增加同到期集中度上限

### 阶段 B：`$500k ~ $1M`

- 仍以当前 IC 结构为主，但不再追求更高单周期组数
- 上线“**多到期梯队**”而不是“更大单次仓位”
- 将 `QQQ` 作为主容量载体，`IWM/GLD` 作为分散和风控补充
- 开始准备组合单执行能力

### 阶段 C：`$1M ~ $3M`

- 把系统升级为“**多 sleeve 组合策略**”
- 当前这套 IC 只是其中一个核心 sleeve，不宜独占全部风险预算
- 在没有组合单、梯队化和容量预算管理之前，不建议直接把现有单策略放大到这个级别
