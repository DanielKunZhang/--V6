# IMPLEMENTATION_PLAN_500K_PLUS

目标：先把 `500k+` 的扩容、风控统一和执行升级做成清晰计划，暂不改交易代码，后续按阶段落地。

## 一、当前状态确认

### 1) 按上一个美股交易日口径，QQQ / IWM 是否可开第一笔实盘

- 当前日期是 `2026-04-12`（北京时间）
- 对应上一个完整的美股交易日是 `2026-04-10`（美东时间，周五）
- 最新监控值：
  - `QQQ HV20 = 23.5%`
  - `IWM HV20 = 24.3%`
  - `GLD HV20 = 36.5%`
- 当前真实开仓阈值：
  - `QQQ <= 25%`
  - `IWM <= 25%`
  - `GLD <= 18%`

结论：

- **QQQ：可以开仓**
- **IWM：可以开仓**
- **GLD：不能开仓**

但这只是 **HV20 维度**。真正首笔实盘开仓还要同时满足：

- 不处于 `VIX` 硬止损恢复等待期
- 不在开仓冷却期
- 存在合适的 `DTE45` 月度/季度链
- 目标腿满足最小权利金要求
- 目标腿通过流动性闸门和分批限制
- 非美股休市/长假前保护窗口

### 2) 当前风控真实口径

- HV20开仓阈值：`QQQ/IWM 25%`，`GLD 18%`（超过即禁止新开仓）
- HV20降杠杆阈值：`22%`（仅在允许开仓时生效）
- HV20硬止损阈值：`39%`
- HV20恢复阈值：`28%`

### 2.5) 当前 DTE 真实口径

- `DTE45` 的真实含义不是“只能选 45 天”
- 当前代码是：**以 45 天为目标，选择最接近 45 的到期日**
- 若该到期链流动性不够，则继续尝试更长的目标层级：`45 → 52 → 59 → 66`
- 所以后续所有文档建议统一写成：`目标 DTE=45，实际选择最接近 45 的可交易到期日`

### 3) 当前需要统一的小差异

- 交易主程序：按 `hv20 > threshold` 才禁止开仓
- 监控器展示：按 `hv20 >= threshold` 显示“禁止开仓”

这会导致“**监控说不能开，但交易代码在等于阈值时仍可能开**”的边界不一致。

## 二、500k+ 扩容实施计划

核心原则：

- `200k ~ 300k` 还是当前策略的容量与执行问题
- `500k+` 开始进入执行架构与组合架构问题
- `1M ~ 3M` 必须做成多梯队、多 sleeve、多预算系统

## 三、资金梯队 → 升级策略

### 阶段 0：`15k ~ 100k`

- 继续沿用当前主策略
- 参数保持 `P3/C6/W9/D45`
- 当前 `F=20x` 可继续使用
- 主要工作：确保邮件、监控、调度、风控稳定

目标：

- 把首笔实盘和前几笔真实成交做干净
- 建立滑点、成交、止盈止损、冷却期的真实记录

### 阶段 1：`100k ~ 200k`

- 仍以当前结构为主
- 引入更严格的分批执行
- 开始记录每标的每腿成交难度
- 把 `QQQ` 作为主容量承载，`IWM/GLD` 更强调分散而非主扩容

升级重点：

- 单批组数限制
- 单日新增腿数统计
- 同到期集中度统计
- 流动性快照留档

### 阶段 2：`200k ~ 300k`

- 自动从 `F=20x` 降到 `E=15x`
- 继续保持 `P3/C6/W9/D45`
- 不优先改结构参数，先稳住执行质量
- 强制分批、限制同一到期日暴露

升级重点：

- 标的级容量预算
- 同到期最大组数限制
- 单日新增名义资金限制
- `QQQ / IWM / GLD` 分别设置不同的流动性闸门

### 阶段 3：`300k ~ 500k`

- 视为当前单策略、单执行架构的上限附近
- 不再追求同一时点继续线性加组
- 开始引入“到期梯队”思路

升级重点：

- 同一标的拆成不同开仓日
- 同一策略拆成不同到期周
- 同日最大新增仓位限制
- 优先降低同步性风险

### 阶段 4：`500k ~ 1M`

- 重点从“加组”转成“组合化扩容”
- 当前 IC 作为核心收益 sleeve
- 额外容量通过多梯队、错峰执行、不同到期批次承接

建议的 sleeve：

- `Core IC sleeve`：当前 `P3/C6/W9/D45`
- `Staggered IC sleeve`：同参数，但不同开仓日/不同到期周
- `Cash buffer sleeve`：保留更多现金缓冲，不追求满载

升级重点：

- 组合单 / 价差单优先
- 同一分钟集中下单限制
- 每个 sleeve 独立容量预算
- `QQQ` 主承载，`IWM/GLD` 偏分散和风控角色

### 阶段 5：`1M ~ 3M`

- 当前 IC 不宜独占全部风险预算
- 进入“组合预算先行、参数优化后置”的阶段
- 真正的重点是治理结构，而不是再找一个更激进的参数

升级重点：

- 分账户 / 分执行通道
- 多 sleeve 并行管理
- 按标的、按到期梯队、按执行时段做预算
- 组合层面控制最大同步回撤

## 四、HV20 / 风控口径统一计划

目标：监控、邮件、主程序、回测、日报、文档全部讲同一套语言。

### 统一项 1：开仓阈值定义

当前建议统一为：

- `QQQ / IWM`: `HV20 > 25%` 禁止开仓
- `GLD`: `HV20 > 18%` 禁止开仓

原因：

- 这是当前主程序的真实交易口径
- 可避免边界上出现“等于阈值时监控禁止、交易却可开”的冲突

### 统一项 2：显示口径

所有对外展示统一成：

- `正常`
- `接近阈值`
- `禁止开仓`
- `降杠杆中`
- `硬止损中`
- `硬止损恢复等待中`

### 统一项 3：优先级顺序

建议统一优先级：

1. `HV20 >= 39%`：硬止损
2. 若已进入硬止损状态，则必须 `HV20 < 28%` 才允许恢复
3. `HV20 > 开仓阈值`：禁止新开仓
4. 在允许开仓前提下，`HV20 > 22%`：降杠杆 / 组数减半
5. 其余条件再进入链筛选、流动性、权利金和容量检查

### 统一项 4：邮件和监控展示

建议所有日报/监控都显示：

- 当前 `HV20`
- 对应阈值
- 当前风控状态
- 是否允许新开仓
- 是否处于降杠杆
- 是否处于硬止损恢复窗口

### 统一项 5：文档与回测映射

后续所有文档统一写法：

- HV20开仓阈值：`QQQ/IWM 25%`，`GLD 18%`（超过即禁止新开仓）
- HV20降杠杆阈值：`22%`（仅在允许开仓时生效）
- HV20硬止损阈值：`39%`
- HV20恢复阈值：`28%`

## 五、执行架构升级路线图

### 路线图 A：先做统计，不急着交易逻辑改造

第一步不改核心策略，只加统计和观测：

- 每标的每次开仓的单批组数
- 每腿实际成交时间差
- 中间价 vs 实际成交价
- 每次开仓的盘口深度快照
- 同到期暴露占比

目的：

- 先知道真正卡住容量的是哪里
- 不要盲目“优化”而过度拟合

### 路线图 B：执行控制层

在统计稳定后，再逐步上控制：

- 单批组数上限
- 单日新增腿数上限
- 单日新增名义资金上限
- 同到期最大组数
- 同一标的的批次间隔

### 路线图 C：订单执行层

再往后升级订单层：

- 从四腿串行转向价差单/组合单优先
- 先做双边价差，再做完整组合单
- 在可行时尽量缩短腿间执行时间差

### 路线图 D：组合层

最后升级到组合治理：

- 多到期梯队
- 多 sleeve
- 标的级容量预算
- 风险预算动态分配

## 六、建议的实施顺序

### 第一步：先稳定当前小资金实盘

- 做首笔真实开仓
- 收集真实滑点和执行数据
- 验证邮件、监控、调度、风控动作一致

### 第二步：先统一风控口径

- 统一监控与主程序的阈值边界定义
- 统一邮件和日报的风控状态文案
- 统一所有文档的表达

### 第三步：先做观测层，再做控制层

- 先统计，再限制
- 先知道问题在哪，再决定怎么改

### 第四步：500k+ 再升级执行架构

- 先到期梯队
- 再 sleeve
- 再组合单
- 最后才考虑是否需要进一步扩展策略复杂度

## 七、最终结论

- 以 `2026-04-10` 的上一个美股交易日口径看，**QQQ 和 IWM 是可以做第一笔实盘开仓的**
- 当前最需要统一的是 **HV20 边界显示与真实交易口径**
- `500k+` 的关键不是继续线性加组，而是 **分层执行、分梯队建仓、分 sleeve 管理**
- `1M ~ 3M` 的重点一定是 **组合治理能力**，而不是再去过度优化结构参数
