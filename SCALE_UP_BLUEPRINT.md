# SCALE_UP_BLUEPRINT

目标：当实盘资金从 `500k` 继续扩到 `1M ~ 3M` 时，保持当前美股多标的 `IC` 系统的收益、回撤和 `Sharpe` 尽量稳定，而不是简单线性加组。

## 核心原则

- 当前核心结构 `P3.0% / C6.0% / Wing9% / DTE45` 仍可保留为主策略骨架
- `500k+` 的关键不再是 `F` 还是 `E`，而是**执行架构、到期梯队、风险预算**
- 放大账户时优先降低**同步性风险**，而不是追求单次建仓规模最大化

## 为什么 500k+ 必须升级

- 同一标的、同一到期、同一时点集中开仓，会放大成交冲击
- 四腿串行执行在大单量下容易出现腿间偏移和净权利金劣化
- 大资金最容易伤害的不是年化，而是回撤控制和 `Sharpe`
- 到 `1M ~ 3M` 后，容量管理必须从“组数控制”升级到“组合预算控制”

## 建议的三层 sleeve 架构

### Sleeve A：核心收益层

- 占组合风险预算：`40% ~ 55%`
- 标的：`QQQ / IWM / GLD`
- 结构：当前主结构 `P3/C6/W9/D45`
- 目标：维持当前策略的主要复利来源

### Sleeve B：错峰执行层

- 占组合风险预算：`25% ~ 35%`
- 标的：仍以 `QQQ` 为主
- 结构：保持同参数，但把建仓日拆到不同交易日、不同到期周
- 目标：降低“同一天建仓、同一天止损、同一天到期”的同步性

### Sleeve C：现金与风控缓冲层

- 占组合风险预算：`15% ~ 30%`
- 用途：留给波动冲击、滚动换仓、流动性紧张时的被动承接
- 目标：避免为了满仓而牺牲执行质量

## 到期梯队设计

- 不建议全部集中在单一 `DTE45` 批次
- 建议形成 `3` 个并行梯队：
  - 梯队 1：本周新开仓
  - 梯队 2：前一至两周已开仓
  - 梯队 3：接近止盈 / 止损 / 提前平仓阶段
- 这样做的核心价值：
  - 降低同到期日暴露
  - 降低同一波动冲击下的组合同步损失
  - 提高资金周转平滑度

## 执行架构升级路线

### 500k ~ 1M

- 强制分批执行
- 给 `QQQ / IWM / GLD` 单独设置批次数和单日上限
- 增加“单次新增腿数”限制
- 开始准备价差单 / 组合单执行通道

### 1M ~ 3M

- 组合单优先，减少四腿裸串行比例
- 分账户或分执行通道承载不同 sleeve
- 每个 sleeve 独立计算容量、风险和执行预算
- 同一标的不同梯队避免在同一分钟集中下单

## 标的层面的建议

### QQQ

- 继续作为主容量承载标的
- 流动性最好，适合承担主要扩容量
- 但也要限制同到期、同批次的过度集中

### IWM

- 继续保留，用于小盘风格分散
- 大资金下要比 `QQQ` 更严格地检查盘口和滑点
- 不建议把 IWM 当成主扩容量通道

### GLD

- 继续保留，用于相关性分散和防御属性
- 但流动性和盘口厚度通常不如 `QQQ`
- 更适合做风险分散 sleeve，不适合承担激进扩容

## 我建议的实盘路线

### 阶段 1：`200k ~ 300k`

- 当前代码继续可用
- 自动降档到 `E=15x`
- 强制分批、限制同到期集中度

### 阶段 2：`500k ~ 1M`

- 当前 IC 仍做核心收益层
- 不再继续增加单期组数
- 上线多到期梯队和错峰建仓

### 阶段 3：`1M ~ 3M`

- 当前 IC 变成“核心 sleeve”之一
- 组合风险预算先行，参数优化后置
- 先解决执行和组合治理，再谈继续追求更高年化

## 结论

- 你的判断是对的：`500k+` 不是简单组数问题
- 当前最优结构参数可以继续做核心，但必须进入“多梯队、多 sleeve、强执行治理”阶段
- 真正决定 `1M ~ 3M` 能否做稳的，不是回测里多一两档参数，而是组合与执行系统是否升级完成
