# Execution Guard 实施计划（Phase 1）

## 目标

- 保持**策略层 = 日频**，不改变当前最优回测的决策频率。
- 新增**执行层巡检**，专门处理实盘落地中的结构异常与状态漂移。
- 优先提升稳定性与可追踪性，避免把系统改成另一套策略。

## 边界

### 策略层：不改

- `HV20` 开仓过滤
- `HV20` 降杠杆
- `HV20` 硬止损 / 恢复
- 价格止损（翼宽 50%）
- 单标的策略权益峰值回撤 `5%`
- 整组 `50%` 止盈
- 到期前 `2` 个交易日平仓

### 执行层：新增

- 开仓后结构完整性检查
- 持仓期间结构一致性巡检
- 本地状态 vs 富途真实持仓对照
- 异常事件记录与去重告警

## 巡检频率

### 开仓后修复窗口

- 第 1 次：开仓后 `5` 分钟
- 第 2 次：开仓后 `15` 分钟左右（相对首轮 +10 分钟）
- 第 3 次：开仓后 `35` 分钟左右（相对次轮 +20 分钟）

说明：

- 当前 Phase 1 先做**检查 + 告警**，不新增复杂自动补腿逻辑。
- 现有开仓流程里的即时补腿机制继续保留；Execution Guard 负责兜底复核。

### 持仓期间

- 交易时段每 `60` 分钟巡检一次
- 若未来验证发现需要更紧，可再升级到 `30` 分钟

### 收盘前

- 增加一次收盘前结构检查
- 用于当天执行事件归档与次日开盘前确认

## Phase 1 实施内容

### 1. `ic_execution_guard.py`

新增独立执行巡检脚本，职责如下：

- 连接 Futu/OpenD，仅读取真实持仓
- 对 `QQQ / IWM / GLD` 逐一检查
- 输出每个标的的执行健康状态：
  - `OK`
  - `WARNING`
  - `CRITICAL`

### 2. 巡检内容

- 富途真实持仓腿数是否为 `4n`
- 同一到期日下腿数量是否一致
- 是否存在残腿 / 少腿 / 组数不一致
- `ic_open_codes.json` 与真实持仓是否匹配
- `ic_open_trade.json` 与真实持仓到期日 / 组数是否匹配
- 是否出现“本地记录有仓位，但富途无仓位”
- 是否出现“富途有 IC 持仓，但本地无记录”

### 3. 告警机制

- 新增执行巡检状态文件，避免同一异常每小时重复轰炸
- 异常首次出现：写入 `execution_event` + 发邮件
- 异常持续存在：只写日志，不重复发同级邮件
- 异常解除：记录 `resolved` 事件

### 4. 调度接入

在 `scheduler.py` 中新增：

- 开仓后 `5 / 15 / 35` 分钟巡检
- 盘中每小时巡检
- 收盘前巡检

## 暂不做（留到 Phase 2）

- 自动补腿增强
- 自动平残腿
- 自动纠正本地状态文件
- 异常日自动从 `60` 分钟切到 `30` 分钟

## 验证方案

### 验证 1：静态验证

- Python 编译通过
- 调度器可正常注册新任务

### 验证 2：只读运行验证

- 手动运行 `ic_execution_guard.py`
- 确认不会误发大量告警
- 确认正常仓位下状态为 `OK`

### 验证 3：灰度上线

- 先上线 Phase 1
- 观察数个交易日
- 若无误报、无噪音，再评估 Phase 2

## 回滚原则

若出现以下任一情况，立即回滚为“只保留日志，不发邮件”：

- 误报频繁
- 同一异常重复告警
- 误识别正常仓位为异常
- 影响主策略运行稳定性

## 核心原则

- **策略层贴近回测**
- **执行层只防事故**
- **先可观测，再自动化**
